# WSOPTV Test Environment Configuration
# WARNING: Use ONLY for testing. Never use in production!

# Test flag
TESTING=true

# Test Database (isolated)
POSTGRES_HOST=localhost
POSTGRES_PORT=5434
POSTGRES_USER=wsoptv_test
POSTGRES_PASSWORD=test_password_only
POSTGRES_DB=wsoptv_test

# Test Redis (isolated)
REDIS_HOST=localhost
REDIS_PORT=6380

# Test MeiliSearch (isolated)
MEILI_HOST=http://localhost:7701
MEILI_MASTER_KEY=test_meili_key_only

# Test JWT
JWT_SECRET_KEY=test_jwt_secret_key_for_testing_only_do_not_use_in_production

# Test Jellyfin (optional, can be mocked)
JELLYFIN_HOST=http://localhost:8096
JELLYFIN_API_KEY=test_jellyfin_api_key
